# For the combined Render service, leave VITE_API_URL and VITE_API_BASE_URL unset.
# The production bundle automatically calls /api on its own origin.
# Set one only when hosting the API elsewhere.
# VITE_API_URL=https://your-api.example.com/api
# VITE_API_BASE_URL=https://your-api.example.com/api
VITE_APP_NAME=EcoSathi
